package com.pradeep.hibernate.dao;

import org.hibernate.Session;

import com.pradeep.hibernate.dto.UserDetailsDTO;
import com.pradeep.hibernate.util.HibernateUtil;

public class ReadData {
	public static void main(String[] args) {
		/*
		 * Session session = HibernateUtil.getSessionFactory().openSession(); session =
		 * HibernateUtil.getSessionFactory().openSession(); UserDetailsDTO
		 * user=(UserDetailsDTO) session.load(UserDetailsDTO.class, 1);
		 * System.out.println(user.getUserName()); session.close();
		 * HibernateUtil.shutdown();
		 */
	}
}
